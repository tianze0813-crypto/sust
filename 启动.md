conda activate sustechpoints
export CUDA_VISIBLE_DEVICES=-1
# 注意: TF 2.15 内置 Keras 2, 必须 unset TF_USE_LEGACY_KERAS, 否则报错
unset TF_USE_LEGACY_KERAS
python main.py

# ---------------------------------------------------------------------------
# 使用说明
#
# 1) 选择数据目录
#    默认读取 <仓库>/data。页面右上角点「文件夹」图标可打开目录选择框，
#    浏览服务器任意目录并扫描其中的 scene：
#      - 选中包含 scene 子目录的目录 -> 作为数据根
#      - 选中某个 scene 目录本身     -> 用其父目录做根并自动打开该 scene
#    选择结果按浏览器会话（cookie）记住，刷新页面不会丢；换浏览器/无痕可各自
#    使用不同目录，互不影响。点「恢复默认目录」可回到 <仓库>/data。
#    （也支持 URL 参数：/?dataset=/abs/path/to/batch 或 .../某个clip）
#
# 2) temp 缓存
#    多点云 scene 的融合结果会缓存到 <仓库>/temp/fused_lidar/。后端退出
#    （Ctrl-C / kill / 正常结束）时自动清空 temp，避免缓存越攒越大。
#    如需保留缓存：启动前 export SUST_CLEAN_TEMP_ON_EXIT=0
#
# 3) 默认数据根
#    可用 export SUST_DATA_ROOT=/abs/path 覆盖默认目录（前端仍可再切换）。